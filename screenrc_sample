# to install:
# add following to rc.local:
# su [user to run as] -c 'cd /home/pi/wherever  ;screen -dmS mcbot -c screenrc'

# set scrollback if you want it and have the memory
# defscrollback 10000

# create more windows
debug on
chdir /home/pi/path_to_cloned_repo
screen -t main
screen -t mcbot ./bot_safe.py
